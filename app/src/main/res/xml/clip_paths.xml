<?xml version="1.0" encoding="utf-8"?>
<!-- The one folder BlazeIt shares through its FileProvider: the picture or file on the shared
     clipboard, so other apps on the phone can paste it. Read-only, and nothing else. -->
<paths>
    <files-path name="clip" path="clip/" />
</paths>
